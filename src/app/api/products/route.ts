import { NextResponse } from 'next/server';

export async function GET(req: Request) {
  // GET endpoint kod kommer leva här
};

export async function POST(req: Request) {
  // POST endpoint kod kommer leva här
};